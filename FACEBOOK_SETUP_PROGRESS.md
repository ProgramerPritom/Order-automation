# 📘 KothaShop.ai — Facebook Integration & Multi-Tenant Setup Progress

**তারিখ ও সময়:** ৬ সেপ্টেম্বর, ২০২৬ | ১৮:০১ (BST)  
**প্রোজেক্ট:** KothaShop.ai (Social Commerce AI Automation Platform)  
**উদ্দেশ্য:** নন-টেকনিক্যাল ক্লায়েন্টদের জন্য ১-ক্লিকে ফেসবুক পেজ কানেকশন এবং সেন্ট্রাল মেটা মাস্টার আর্কিটেকচার বাস্তবায়ন।

---

## 🚀 ১. আজ পর্যন্ত যা যা সম্পন্ন হয়েছে (Completed Milestones)

### ক. মেটা মাস্টার অ্যাপ তৈরি (Master Meta App)
* **অ্যাপের নাম:** `Kotha Shop AI automation`
* **অ্যাপের ধরন (App Type):** `Business` (মেসেঞ্জার, ইনস্টাগ্রাম, হোয়াটসঅ্যাপ ও মার্কেটিং এপিআই একসাথে পাওয়ার জন্য)।
* **Master App ID:** `1088885870322128` (আমাদের `.env` ফাইলে সংরক্ষিত)।
* **অ্যাপের মোড:** `Development` (ডেভেলপমেন্ট ও টেস্টিং মোডে আছে)।

---

### খ. ওয়েবহুক (Webhook) কনফিগারেশন সম্পন্ন
* **Callback URL:** `https://7rh6drfk-3001.asse.devtunnels.ms/api/webhooks/meta` (VS Code Dev Tunnel দিয়ে লাইভ HTTPS লিঙ্ক)।
* **Verify Token:** `saas_meta_webhook_secret_2026`
* **হ্যান্ডশেক স্ট্যাটাস:** ✅ **Verified & Saved** (মেটা সফলভাবে আমাদের ব্যাকএন্ডের সাথে কানেক্ট হয়েছে)।

---

### গ. পেজ সাবস্ক্রিপশন ও টোকেন জেনারেশন (Little Joys)
* **টেস্ট পেজ:** `Little Joys`
* **পেজ আইডি (Page ID):** `1374129259109200`
* **সাবস্ক্রাইব করা ইভেন্টসমূহ:**
  1. `messages` (গ্রাহকের মেসেজ পাওয়ার জন্য)
  2. `messaging_postbacks` (বাটন ও কুইক রিপ্লাই ক্লিক পাওয়ার জন্য)
  3. `feed` (পেজের পোস্টে করা কমেন্ট পাওয়ার জন্য)
  4. `message_reactions` (ইমোজি রিঅ্যাকশন পাওয়ার জন্য)
* **Page Access Token:** জেনারেট করে `.env` ফাইলে `META_ACCESS_TOKEN` হিসেবে সেভ করা হয়েছে।

---

### ঘ. ডাটাবেজ ইন্টিগ্রেশন (PostgreSQL)
* পেজ `Little Joys` কে সফলভাবে আমাদের ক্লায়েন্ট/টেন্যান্ট অ্যাকাউন্ট (`Sahil Shoe Store` / `badruzzamanpritom@gmail.com`)-এর সাথে যুক্ত করা হয়েছে।
* **ডাটাবেজ টেবিল:** `channels`
  * `platform`: `'facebook'`
  * `channel_identifier`: `'1374129259109200'`
  * `channel_name`: `'Little Joys'`
  * `ai_active`: `true`
  * `webhook_verified`: `true`
* এখন **[http://localhost:3001/dashboard/channels](http://localhost:3001/dashboard/channels)** পেজে গেলে এই পেজটি সম্পূর্ণ লাইভ দেখা যাচ্ছে।

---

### ঙ. ইনভয়েস ও ক্যাশ মেমো প্রিন্টার (Print-Ready Cash Memo)
* `@media print` সাপোর্টসহ প্রফেশনাল ক্যাশ মেমো প্রিন্ট সিস্টেম যুক্ত করা হয়েছে।
* পার্সেলের গায়ে লাগানোর জন্য কাস্টমার তথ্য, ফোন, ফুল ডেলিভারি ঠিকানা, আইটেম লিস্ট, সাবটোটাল, ডেলিভারি চার্জ এবং ক্যাশ অন ডেলিভারি (COD) অ্যামাউন্ট ও বারকোড প্রদর্শন।
* ব্রাউজারের প্রিন্ট ডায়লগ ওপেন করলে ব্যাকগ্রাউন্ড বা সাইডবার ছাড়া শুধুমাত্র মেমোটি ক্লিয়ার প্রিন্ট হবে।

---

### চ. মেটার কঠোর ১-বার প্রাইভেট রিপ্লাই রুল এনফোর্সমেন্ট
* প্রতি কমেন্টের বিপরীতে গ্রাহকের ইনবক্সে শুধুমাত্র **১টি প্রাইভেট বার্তা** পাঠানো সম্ভব।
* একবার ইনবক্সে পাঠানো হলে ড্যাশবোর্ডে বাটনটি স্বয়ংক্রিয়ভাবে ডিসেবল হয়ে যাবে এবং দেখাবে: `ইনবক্সে পাঠানো হয়েছে ✓`।
* ব্যাকএন্ডে ডুপ্লিকেট রিকোয়েস্ট ফিল্টারিং যুক্ত করা হয়েছে যাতে মেটার `#10900` এরর না আসে।

---

### ছ. ১-ক্লিক কুরিয়ার পার্সেল বুকিং (Steadfast & Pathao Courier)
* ড্যাশবোর্ডে প্রতি অর্ডারের পাশে **"কুরিয়ার"** বাটন যুক্ত।
* ক্লিক করলেই `/api/orders/courier` API-এর মাধ্যমে স্বয়ংক্রিয়ভাবে স্টেডফাস্ট/পাঠাও-তে বুকিং হয়ে রিয়েল ট্র্যাকিং কোড জেনারেট হবে এবং অর্ডার স্ট্যাটাস `shipped` এ আপডেট হবে।

---

## 🧠 ২. আমাদের মাল্টি-টেন্যান্ট (১০০+ ক্লায়েন্ট) আর্কিটেকচার কীভাবে কাজ করবে?

সাধারণ ক্লায়েন্টরা কোনো কোড, পেজ আইডি বা টোকেন বুঝবে না। তারা যেভাবে ১-ক্লিকে পেজ কানেক্ট করবে:

1. **ক্লায়েন্টের অভিজ্ঞতা (Frontend):**
   * ক্লায়েন্ট ড্যাশবোর্ডে এসে ক্লিক করবে: **`[ 🔵 ১-ক্লিকে ফেসবুক পেজ কানেক্ট করুন ]`**।
   * ফেসবুকের অফিসিয়াল সিকিউর পপআপ ওপেন হবে (যেখানে আমাদের Master App ID `1088885870322128` লোড হবে)।
   * ক্লায়েন্ট তার পেজটিতে টিক দিয়ে **"Continue"** চাপবে।

2. **বিহাইন্ড দ্য সিন স্বয়ংক্রিয় ব্যাকএন্ড ফ্লো (Behind-the-Scenes):**
   * ফেসবুক আমাদের `/api/auth/facebook/callback` রাউটে একটি রিডাইরেক্ট কোড পাঠাবে।
   * ব্যাকএন্ড স্বয়ংক্রিয়ভাবে `GET /me/accounts` কল করে ক্লায়েন্টের **Page ID**, **Page Name** ও **Long-Lived Access Token** নিয়ে নেবে।
   * ব্যাকএন্ড স্বয়ংক্রিয়ভাবে `POST /{page_id}/subscribed_apps` কল করে ক্লায়েন্টের পেজে Webhook সাবস্ক্রিপশন অন করে দেবে।
   * ব্যাকএন্ড ক্লায়েন্টের `tenant_id`-এর অধীনে ডাটাবেজে পেজটি সেভ করে ফেলবে।

3. **মেসেজ ও কমেন্ট রাউটিং:**
   * যেকোনো পেজে কাস্টমার মেসেজ দিলে মেটা পাঠাবে আমাদের একটিমাত্র সেন্ট্রাল Webhook-এ (`/api/webhooks/meta`)।
   * ব্যাকএন্ড পেজ আইডি দেখে সাথে সাথে ডাটাবেজ থেকে বের করবে এটি কোন ক্লায়েন্টের পেজ।
   * সেই ক্লায়েন্টের প্রোডাক্ট ক্যাটালগ ও রুলস নিয়ে **n8n AI Engine**-এ পাঠাবে।
   * AI থেকে রেসপন্স এলে ওই নির্দিষ্ট পেজের টোকেন দিয়ে কাস্টমারকে স্বয়ংক্রিয়ভাবে রিপ্লাই করে দেবে।

---

## 📌 ৩. পরবর্তী পদক্ষেপ (Next Steps to Execute)

পরবর্তী কাজের সময় আপনি শুধু বললেই আমরা এই ধাপগুলো সম্পন্ন করব:

1. **App Secret যুক্ত করা:**
   * মেটা ডেভেলপার ড্যাশবোর্ড থেকে **App settings ➔ Basic ➔ App secret** কপি করে `.env`-এ `META_APP_SECRET` আপডেট করা।
2. **১-ক্লিক ফেসবুক OAuth ব্যাকএন্ড রাউট তৈরি:**
   * `/api/auth/facebook/login` (লগইন ডায়লগ ওপেন করার জন্য)।
   * `/api/auth/facebook/callback` (স্বয়ংক্রিয় টোকেন সংগ্রহ, পেজ সাবস্ক্রাইব ও ডাটাবেজ সেভ করার জন্য)।
3. **ড্যাশবোর্ড UI আপডেট:**
   * `/dashboard/channels` পেজে ক্লায়েন্টদের জন্য বড় ও আকর্ষণীয় **"🔵 ১-ক্লিকে ফেসবুক পেজ কানেক্ট করুন"** বাটন যুক্ত করা।
4. **এন্ড-টু-এন্ড লাইভ টেস্ট:**
   * আসল ফেসবুক আইডি দিয়ে পেজে মেসেজ পাঠানো ➔ Webhook ➔ n8n AI প্রসেসিং ➔ মেসেঞ্জারে অটো-রিপ্লাই নিশ্চিত করা।

---
*এই ফাইলটি আপনার প্রোজেক্ট রুটে `FACEBOOK_SETUP_PROGRESS.md` নামে সংরক্ষিত আছে।*
